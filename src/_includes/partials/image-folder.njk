{% set images = (imageFolder | folderImages) %}

{% if images.length %}

    {% for image in images %}
        <img style="width: 100%; padding-bottom: 50px;"src="{{ image }}">
    {% endfor %}

{% endif %}