<!DOCTYPE html>

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title> {{ header }} </title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body {% if "garden" in page.url %} class="garden" {%endif%} {% if page.url == '/' %} class="home-background"{%endif%}>
    
    
        {% if empty %} {# if the page wants a widscreen layout#}
            <div class="full-container">
        {% else %} {# else its just regular #}
            <div class="container">
        {%endif%}

        <main>
        {% if page.url == "/garden/" %} {# for the banner title on garden#}
        <div class="banner">
             <h1>{{ header }}</h1>
        </div>
        {% else %}
            {% if "garden" in page.url %}
                {% include "partials/header.njk" ignore missing %}
            {%else%}
                {% include "partials/portfolio-header.njk" ignore missing %}
            {%endif%}
            
            {% if imgHero %}
                <figure>
                    <img class="img" src="/images/{{ imgHero }}" alt tooltip="{{ imgCaption }}">
                    <figcaption> {{ imgCaption }} </figcaption>
                </figure>
            {% endif%}
            {% if "shrines" not in page.url %}
                {% if not empty %}
                    <h1 class="head">{{ header }}</h1>
                {% endif%}
            {%endif%}
        {% endif %}

                {{ content | safe }}
            
        </main>

    </div>

    </body>
</html>
